<Window x:Class="EloCalculator.GUI.Games"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EloCalculator.GUI"
        mc:Ignorable="d"
        Title="EloCalculator: Games" Height="450" Width="800">

    <Grid>
        <local:Menu/>

        <DataGrid
            Name = "DataGrid"
            AlternatingRowBackground = "LightSteelBlue"  
            AlternationCount = "2"
            AutoGenerateColumns = "False"
            PreviewKeyDown="PreviewKeyDownHandler"
            Margin="0,20,0,0">

            <DataGrid.Columns>
                <DataGridTextColumn
                    Header = "Game ID"
                    Width="1*"
                    Binding = "{Binding Path=ID, Mode=OneWay}" />

                <DataGridTextColumn
                    Header = "White ID"
                    Width="1*"
                    Binding = "{Binding Path=WhitePlayer.ID, Mode=OneWay}" />

                <DataGridTextColumn
                    Header = "Black ID"
                    Width="1*"
                    Binding = "{Binding Path=BlackPlayer.ID, Mode=OneWay}" />

                <DataGridTextColumn
                    Header = "Result"
                    Width="1*"
                    Binding = "{Binding Path=Result, Mode=OneWay}" />

                <DataGridTextColumn
                    Header = "Date and Time"
                    Width="1*"
                    Binding = "{Binding Path=DateTime, Mode=OneWay}" />

                <DataGridCheckBoxColumn
                    Header = "Rated"
                    Width="1*"
                    Binding = "{Binding Path=Rated, Mode=OneWay}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
